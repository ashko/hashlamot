# העלאה לפרודקשן — צעד אחר צעד

מסלול מלא מאפס עד שההורים משתמשים באפליקציה. **זמן: כשעה**, מזה רוב הזמן הוא שלב 8 (ישיבה איתם).

לפני שמתחילים — הכל בחינם, ואין צורך בכרטיס אשראי בשום שלב.

---

## 1 · פרויקט Supabase

1. היכנס ל־[supabase.com](https://supabase.com) → **New project**.
2. **Name:** `hashlamot` · **Region:** `Central EU (Frankfurt)` — הכי קרוב לישראל.
3. בחר סיסמה לבסיס הנתונים ושמור אותה במקום בטוח. **לא תצטרך אותה לאפליקציה**, רק אם תרצה להתחבר ישירות ל־DB.
4. המתן ~2 דקות עד שהפרויקט קם.

## 2 · הרצת המיגרציות

**SQL Editor** בתפריט הצד → **New query**. הרץ את חמשת הקבצים **לפי הסדר**, אחד בכל פעם (העתק־הדבק את התוכן ואז **Run**):

| # | קובץ | מה הוא עושה |
|---|---|---|
| 1 | `supabase/migrations/0001_schema.sql` | טבלאות, מחלקות, יחידות |
| 2 | `supabase/migrations/0002_rls.sql` | הרשאות ובידוד לפי משק בית |
| 3 | `supabase/migrations/0003_functions.sql` | צירוף, מיזוג רשימה, עדכון אופליין |
| 4 | `supabase/migrations/0004_seed_catalog.sql` | 272 המוצרים |
| 5 | `supabase/migrations/0005_storage.sql` | אחסון תמונות |

כל אחד צריך לחזור `Success. No rows returned`.

> המיגרציות נבדקו על Postgres 16 אמיתי מקצה לקצה. אם משהו נכשל — עצור ואל תמשיך לקובץ הבא, כי כל אחד תלוי בקודמו.

**בדיקה שהכל עלה** — הרץ בחלון חדש:

```sql
select
  (select count(*) from departments) as departments,   -- 15
  (select count(*) from units)       as units,         -- 12
  (select count(*) from pg_proc where proname = 'build_list') as build_list;  -- 1
```

## 3 · הפעלת כניסה אנונימית

**Authentication → Sign In / Providers → Anonymous Sign-ins → Enable → Save.**

בלי זה האפליקציה לא תוכל להיכנס בכלל. אין באפליקציה סיסמאות או אימיילים — המכשיר נכנס אנונימית ומצטרף למשק הבית עם קוד חד־פעמי.

## 4 · העתקת שני המפתחות

**Project Settings → API**. קח:

- **Project URL** — משהו כמו `https://abcdefgh.supabase.co`
- **anon / public key** — מחרוזת ארוכה שמתחילה ב־`eyJ...`

> שניהם ציבוריים בכוונת המתכנן. ה־`anon key` נשלח לדפדפן של כל מבקר, וכל טבלה מאחוריו מוגנת ב־RLS. **אל תיקח את ה־`service_role` key** — הוא כן סודי ואין בו צורך כאן.

## 5 · הגדרת הריפו ב־GitHub

### 5א · המשתנים

**Settings → Secrets and variables → Actions → לשונית Variables → New repository variable**, פעמיים:

| Name | Value |
|---|---|
| `VITE_SUPABASE_URL` | ה־Project URL משלב 4 |
| `VITE_SUPABASE_ANON_KEY` | ה־anon key משלב 4 |

*(Variables ולא Secrets — הערכים ציבוריים ממילא, וכך אפשר לראות אותם ולוודא שלא נפלה טעות הדבקה.)*

### 5ב · Pages

**Settings → Pages → Source: GitHub Actions.**

הריפו חייב להיות **ציבורי** — Pages בריפו פרטי דורש מנוי בתשלום. הקוד גלוי; הנתונים לא.

## 6 · יצירת הענף הראשי והפריסה הראשונה

### רקע קצר, במילים פשוטות

הקוד שלי יושב כרגע ב"ענף" (branch) בשם `claude/family-shopping-list-app-oz4n59`. ענף הוא בסך הכל **גרסה של הקוד**. ריפו יכול להחזיק כמה גרסאות במקביל.

יש בריפו גם מושג של **הענף הראשי** — הגרסה ה"אמיתית". המוסכמה היא לקרוא לו `main`.

המנגנון שבונה ומעלה את האתר (ה־workflow) מוגדר לרוץ **רק כשמשהו נכנס ל־`main`**. וכרגע `main` פשוט לא קיים. לכן שום דבר לא עולה לאוויר.

מה שצריך לעשות: **ליצור את `main` כעותק של הענף הקיים.** אפשר לגמרי דרך האתר, בלי להתקין כלום.

---

### 6א · יצירת הענף `main`

1. פתח את הריפו: **`github.com/ashko/hashlamot`**
2. מעל רשימת הקבצים, בצד שמאל, יש **כפתור עם אייקון של ענף** ושם הענף הנוכחי (`claude/family-shopping-list-app-oz4n59`). לחץ עליו.
3. נפתח חלון קטן עם שדה חיפוש. **הקלד בו:** `main`
4. לא יימצא ענף כזה, ובמקום זה תופיע שורה בתחתית:
   **`Create branch: main from 'claude/family-shopping-list-app-oz4n59'`**
   לחץ עליה.

זהו. יש עכשיו ענף `main` עם בדיוק אותו קוד.

### 6ב · הפיכת `main` לענף הראשי

5. **Settings** (בסרגל העליון של הריפו) → בעמוד **General**, גלול עד **Default branch**.
6. ליד שם הענף יש **אייקון של שני חצים מוחלפים** (⇄). לחץ עליו.
7. בחר מהרשימה **`main`** → **Update** → **I understand, update the default branch**.

### 6ג · הפעלת הפריסה

יצירת הענף אמורה להפעיל את הבנייה לבד. בוא נוודא:

8. לחץ על לשונית **Actions** בסרגל העליון.
9. אם אתה רואה שורה בשם **Deploy** עם עיגול צהוב (רץ) או ✓ ירוק (הסתיים) — מצוין, דלג ל־6ד.
10. אם הרשימה ריקה או שאין ריצה של Deploy:
    - בעמודה השמאלית לחץ על **Deploy**
    - מימין יופיע כפתור **Run workflow** → לחץ → ובחלונית שנפתחת לחץ שוב **Run workflow**
    - רענן את העמוד אחרי כמה שניות

### 6ד · מעקב

11. לחץ על שורת ה־**Deploy** כדי לראות את ההתקדמות. יש שני שלבים, `build` ואחריו `deploy`.
12. אחרי **כדקה־שתיים** שניהם יקבלו ✓ ירוק.
13. הכתובת של האפליקציה:

    ### `https://ashko.github.io/hashlamot/`

    (היא מופיעה גם ב־**Settings → Pages**, למעלה, בתוך מסגרת ירוקה.)

> **שים לב:** בפעם הראשונה בלבד, לפעמים לוקח לכתובת עוד 2–3 דקות להתחיל לעבוד גם אחרי שה־workflow ירוק. אם קיבלת 404 — חכה קצת ורענן.

---

### מכאן והלאה

כל שינוי שייכנס ל־`main` יפרוס את עצמו אוטומטית. אתה לא צריך לגעת בשום דבר, וההורים בוודאי שלא — האפליקציה מתעדכנת אצלם בפתיחה הבאה.

### אם משהו מהשלב הזה לא הסתדר

| מה קרה | מה לעשות |
|---|---|
| ה־Deploy נכשל עם `VITE_SUPABASE_URL is not set` | חזור ל־5א. הבדיקה הזאת רצה **לפני** הבנייה בכוונה, כדי שלא תגלה את הבעיה רק כשההורים פותחים מסך שגיאה. אחרי שתתקן: Actions → Deploy → **Re-run all jobs** |
| ה־Deploy נכשל עם משהו על `Pages` | שלב 5ב לא בוצע. **Settings → Pages → Source: GitHub Actions** |
| אין בכלל לשונית Actions | הריפו פרטי בתוכנית חינמית. **Settings → General → למטה → Change repository visibility → Public** |
| הכפתור **Run workflow** לא מופיע | 6ב לא הושלם — `main` חייב להיות הענף הראשי כדי שהכפתור יופיע |

---

*אם עדיף לך דרך הטרמינל, אלה שלוש הפקודות המקבילות:*

```bash
git checkout claude/family-shopping-list-app-oz4n59
git checkout -b main
git push -u origin main
```

*ואז 6ב באתר, כרגיל.*

## 7 · הקמת הבית וחיבור הטלפונים

### במכשיר שלך

1. פתח את הכתובת מהשלב הקודם.
2. **"אני מקים את זה"** → שם הבית ושמך → **הקמה**.
   הקטלוג (272 מוצרים) נזרע אוטומטית.

### בטלפון של אמא

3. אצלך: **⚙ הגדרות → פתיחת חלון צירוף** → יופיע קוד בן 8 תווים, תקף 10 דקות.
4. בטלפון שלה, ב־**Safari** (לא כרום — רק ספארי יכול להתקין למסך הבית ב־iOS): אותה כתובת → **"יש לי קוד"** → הקוד → שם → **"מכינה רשימות"**.

### בטלפון של אבא

5. אצלך: **פתיחת חלון צירוף** שוב — הקוד הקודם נשרף בשימוש.
6. בטלפון שלו, ב־Safari: הקוד החדש → שם → **"הולך לסופר"**.

### התקנה למסך הבית — בשני הטלפונים

7. ב־Safari: כפתור **שיתוף** (הריבוע עם החץ) → גלול → **"הוספה למסך הבית"** → **הוסף**.

> **אל תדלג על זה.** בלי התקנה למסך הבית: אין התראות פוש ב־iOS, האפליקציה נפתחת עם סרגלי הדפדפן, והם צריכים לזכור כתובת במקום להקיש על אייקון. גרור את האייקון למסך הבית הראשון, ליד וואטסאפ.

## 8 · הרשימה הראשונה — לעשות איתה, לא בשבילה

הסיכון הגדול ביותר במוצר הוא לא טכני: שאמא תפתח מסך ריק ותחזור לדף ולעט.

שב איתה עם קפה. **אתה** מקליד את המנה הראשונה בזמן שהיא מכתיבה, **היא** מקלידה את השנייה, ואת השלישית היא מסיימת לבד. הראה לה שהשדה מקבל הכתבה קולית — לגיל הזה זה בדרך כלל מהיר יותר מהמקלדת.

זהו. מכאן זה מצטבר לבד.

---

## אחרי ההעלאה

**עדכונים.** כל דחיפה ל־`main` נפרסת אוטומטית. הם לא צריכים לעשות כלום — האפליקציה מתעדכנת בפתיחה הבאה.

**ה־keepalive.** ה־workflow השני רץ כל יום ב־06:17 UTC ושומר על פרויקט Supabase ער. אל תכבה אותו: פרויקט חינמי נכנס להשהיה אחרי שבוע ללא פעילות, והשימוש כאן שבועי בדיוק. חופשה או חג ישברו את האפליקציה בדיוק כשהם ינסו להשתמש בה.

בדוק פעם ראשונה שהוא ירוק: **Actions → Keep Supabase awake → Run workflow**.

**סדר המחלקות.** אל תנסה לנחש את המסלול של אבא. אחרי שלוש קניות מלאות האפליקציה תציע לו בעצמה את הסדר שהוא באמת עובר, לפי הזמנים שבהם סימן מוצרים. עד אז הוא יכול לתקן בהקשה על ↑↓ ליד כותרת המחלקה, בתוך מסך הקנייה.

---

## אם משהו נשבר

| מה רואים | מה קרה | מה עושים |
|---|---|---|
| מסך "חסרה הגדרה" | המשתנים לא הגיעו ל־build | שלב 5א, ואז **Actions → Deploy → Re-run** |
| "קוד לא תקין, או שחלון הצירוף נסגר" | הקוד חד־פעמי, או שעברו 10 דקות | פתח חלון צירוף חדש |
| דף לבן | ה־`base` לא תואם לשם הריפו | הריפו חייב להיקרא `hashlamot`, או ערוך `BASE_PATH` ב־`deploy.yml` |
| האפליקציה נפתחת בדפדפן ולא במסך מלא | לא הותקנה למסך הבית | חזור לשלב 7 |
| אין התראות | פוש ב־iOS דורש התקנה למסך הבית | שלב 7. בינתיים "שליחה גם בוואטסאפ" |
| שגיאת התחברות אחרי שבוע+ של חוסר שימוש | הפרויקט בהשהיה | Supabase → **Restore project**, ובדוק שה־keepalive רץ |

**להראות למישהו בלי להקים כלום:** הוסף `?demo=1` לכתובת. האפליקציה רצה על נתונים מדומים, בלי Supabase, בלי צירוף.
