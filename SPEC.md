# השלמות — אפיון מוצר

> אפליקציית רשימת קניות משותפת להורים. אמא מתכננת את הבישולים, אבא קונה, שניהם רואים את אותו הדבר בזמן אמת.

**גרסה:** 0.2 — תשתית על GitHub + Supabase
**סטטוס:** ממתין לאישור לפני תחילת פיתוח

---

## 1. הבעיה

מדי יום רביעי אמא מתחילה בהכנות לבישולי סוף השבוע. היא יודעת אילו מנות היא רוצה להכין, ומהמנות האלה היא גוזרת בראש רשימת מרכיבים, כותבת אותה ביד על דף, ומעבירה לאבא. אבא לוקח את הדף לסופר (חצי חינם) ועובר בין המדפים.

| הכאב | מה קורה היום |
|---|---|
| עבודה כפולה | אותם מרכיבים נכתבים מחדש כל שבוע, בכתב יד |
| סדר לא נכון | הרשימה מסודרת לפי מתכון, אבל הסופר מסודר לפי מחלקות — אבא מסתובב הלוך ושוב |
| אין ערוץ חזרה | אם מוצר חסר במדף, אבא צריך להתקשר, או לנחש, או לוותר |
| שכחות | פריט שנשמט מהדף מתגלה רק כשמתחילים לבשל |
| אין זיכרון | "כמה עגבניות קונים לקוגל?" — התשובה נמצאת רק בראש של אמא |

## 2. המטרה

לצמצם את התכנון של אמא מ־20 דקות של כתיבה ידנית לפחות מ־3 דקות של הקשות, ולתת לאבא רשימה שמסודרת לפי הדרך שהוא הולך בחצי חינם — עם ערוץ חזרה חי כשמשהו חסר.

**מדדי הצלחה:**

1. אמא בונה רשימה שלמה לשבת בפחות מ־3 דקות, לבד, בלי עזרה.
2. אבא משלים קנייה בלי אף שיחת טלפון של הבהרה.
3. אפס פריטים נשכחים בשבועיים רצופים.
4. שניהם משתמשים באפליקציה שבוע רביעי ברציפות — בלי שנזכיר להם.

המדד הרביעי הוא היחיד שבאמת חשוב. דף ועט זה ממשק מצוין; אפליקציה מנצחת אותו רק אם היא מהירה יותר ממנו כבר בשבוע השני.

## 3. המשתמשים

| מי | תפקיד במערכת | מה הוא עושה |
|---|---|---|
| **אמא** | `planner` | בוחרת מנות, מאשרת רשימה, עונה כשחסר משהו |
| **אבא** | `shopper` | מקבל רשימה, מסמן מה נקנה, מדווח על חוסרים |
| **אתה** | `admin` | הקמה ראשונית, קטלוג מוצרים, תמיכה מרחוק |

הפרדת התפקיד השלישי היא לא פרט טכני — היא הנחת יסוד עיצובית. אף אחת מפעולות התחזוקה לא נופלת עליהם. הם רואים שני מסכים בסך הכל.

## 4. המסע — יום רביעי

זה לב האפיון. כל שאר המסמך משרת את הרצף הזה.

### 4.1 אמא, בוקר יום רביעי

1. פותחת את האפליקציה. שאלה אחת גדולה על המסך: **"מה מבשלים לשבת?"**
2. מתחתיה רשת של המתכונים שלה, כל אחד עם תמונה ושם. היא מקישה על שניצל, מרק עוף, קוגל, סלט חצילים. כל אחד שנבחר מקבל מסגרת ירוקה ו־✓.
3. מתכון חדש? כפתור **"+ מנה חדשה"** → שם, תמונה, ואז מרכיבים בחיפוש־תוך־כדי־הקלדה מתוך הקטלוג. **פעם אחת בלבד** — בשבוע הבא המתכון כבר שם עם כל המרכיבים.
4. מקישה **"הכנת רשימה"**. המערכת ממזגת את כל המרכיבים, מאחדת כפילויות, מחברת כמויות, ומסדרת לפי מחלקות.
5. מסך אישור: *"23 מוצרים"*. אפשר לשנות כמות, למחוק, ולהוסיף דברים שלא קשורים למתכון — יש שדה **"+ להוסיף עוד משהו"** קבוע למעלה.
6. מקישה **"לשלוח לאבא"**.

### 4.2 אבא, בסופר

1. מקבל התראה: *"אמא שלחה רשימה — 23 מוצרים"*. מקיש, האפליקציה נפתחת ישר על הרשימה.
2. הרשימה מסודרת לפי המחלקות **בסדר שהוא הולך בחצי חינם**. כל מחלקה היא כותרת גדולה וצבעונית.
3. כל שורה: תמונת המוצר, שם גדול, מותג, כמות. הקשה על השורה כולה = **נקנה**. השורה הופכת לירוקה עם ✓ ודוהה.
4. משהו לא בנמצא — שני כפתורים בשורה: **"חסר"** ו־**"החלפתי"**. "החלפתי" פותח שדה קצר ואפשרות לצלם את מה שהוא כן לקח.
5. למעלה, קבוע: פס התקדמות — *"12 מתוך 23"*.
6. בסוף — כפתור אחד גדול: **"סיימתי"**.

### 4.3 אמא, במקביל

- רואה בזמן אמת מה כבר נקנה. לא צריכה לשאול.
- כשאבא מסמן "חסר" היא מקבלת התראה מיידית: *"חסר: פטרוזיליה"* — ושלושה כפתורי תשובה: **"קח כוסברה במקום"** / **"לא צריך"** / **"תכתוב לי"**.
- התשובה מופיעה אצל אבא על השורה עצמה, תוך שניות.
- כשהוא מסיים: *"אבא סיים. נקנו 21, חסרים 2."*

## 5. הסידור לפי חצי חינם

הדרישה המרכזית: **הרשימה מסודרת לפי הסופר, לא לפי המתכון.**

### 5.1 סדר ברירת מחדל

הסדר הבא מותאם למתכונת של חצי חינם — ירקות ופירות בחזית, דלפקי המעדנייה והקצבייה מרוכזים, מקררי הקיר אחריהם, ומוצרי הלא־מזון בסוף.

| # | מחלקה | # | מחלקה |
|---|---|---|---|
| 1 | פירות וירקות | 9 | שמן, רטבים וממרחים |
| 2 | מאפייה ולחמים | 10 | חטיפים, מתוקים ודגני בוקר |
| 3 | גבינות ומעדנייה | 11 | משקאות, קפה ותה |
| 4 | בשר, עוף ודגים | 12 | קפואים |
| 5 | חלב וביצים | 13 | ניקיון, כביסה וחד״פ |
| 6 | שימורים ומזון יבש | 14 | טואלטיקה ופארם |
| 7 | אורז, פסטה וקטניות | 15 | שונות |
| 8 | תבלינים, אפייה וסוכר | | |

> **הסדר הזה הוא הנחה מושכלת, לא ידיעה.** סניפי חצי חינם שונים זה מזה. שאלה אחת לאבא — "מה אתה עובר קודם, הגבינות או העוף?" — או גרירה אחת במסך ההגדרות, ונסגר.

### 5.2 התאמה לסופר של אבא

מסך אחד בהגדרות: רשימת המחלקות עם ידית גרירה. אבא גורר פעם אחת, והסדר תקף לכל הרשימות מכאן והלאה. מחלקה ללא פריטים ברשימה הנוכחית לא מוצגת.

### 5.3 איך מוצר מקבל מחלקה

- **מוצרי הקטלוג הזרוע** (~250 מוצרים נפוצים) מגיעים עם מחלקה משויכת מראש.
- **מוצר חדש שאמא יוצרת** — אחרי הקלדת השם מופיעה רשת של 15 אריחים גדולים עם אייקון וצבע. הקשה אחת, לא רשימה נפתחת.
- דילגה? המוצר נוחת ב"שונות" בסוף הרשימה. הוא לא הולך לאיבוד.

## 6. מיזוג כמויות

כששני מתכונים דורשים את אותו מוצר, הוא מופיע פעם אחת עם הכמות המחוברת.

- כמויות מתחברות רק בתוך אותה משפחת יחידות: **משקל** (ק״ג/גרם), **נפח** (ליטר/מ״ל), **ספירה** (יחידה, חבילה, קופסה, צרור, שקית — כל אחת נפרד).
- ק״ג ממתכון אחד + גרם מאחר → מומרים, מחוברים, מוצגים ביחידה הנוחה: `1000 + 500 = 1.5 ק״ג`.
- יחידות שלא מתחברות מוצגות זו לצד זו: `עגבניות — 2 ק״ג + 3 יחידות`.

**ניסוח אנושי.** לא `0.5 ק"ג` אלא **"חצי קילו"**. לא `1.5` אלא **"קילו וחצי"**. שברים נפוצים במילים.

**מקור המרכיב.** מתחת לכל שורה, בקטן: *"לשניצל, למרק"*.

המיזוג מתבצע ב־Postgres — ראה סעיף 11.3.

## 7. מצבי מוצר

| מצב | מי מסמן | מראה אצל אבא | מה קורה אצל אמא |
|---|---|---|---|
| `pending` — לקנות | ברירת מחדל | לבן, ריבוע ריק | נספר ב"נשאר" |
| `bought` — נקנה | אבא | ירוק, ✓, טקסט דהוי | מתקדם בפס ההתקדמות |
| `missing` — חסר | אבא | אדום, ✗ | **התראה מיידית** + כפתורי תשובה |
| `substituted` — הוחלף | אבא | כתום, טקסט התחליף ותמונה | התראה + פירוט מה נלקח |

המצב אף פעם לא מקודד בצבע בלבד — תמיד צבע **וגם** אייקון **וגם** שינוי טקסט.

## 8. עקרונות ממשק לגיל השלישי

מספרים, לא תארים. אלה מחייבים.

**טיפוגרפיה**
- גוף טקסט: **20px** בסיס. הגדרה גלובלית: רגיל 20 / גדול 23 / **ענק 27**. ברירת המחדל *גדול*.
- שם מוצר: **26px** / 600. כותרת מחלקה: **30px** / 800, דביקה בראש הגלילה. כמות: **24px** / 700, ספרות ברוחב אחיד.
- ללא טקסט מתחת ל־17px בשום מקום.

**מגע**
- אזור הקשה מינימלי **68×68px**. שורת מוצר **96px** גובה, כל הרוחב לחיץ.
- מרווח מינימלי בין יעדי הקשה: 12px.
- אין החלקה, אין הקשה כפולה, אין לחיצה ארוכה כפעולה יחידה.

**ניווט**
- שני מסכים ראשיים לכל משתמש. אין תפריט המבורגר, אין תפריטים מקוננים.
- פעולה ראשית **אחת** לכל מסך, כפתור ברוחב מלא בתחתית.
- כפתור חזרה תמיד גלוי, תמיד באותו מקום. אין חלונות קופצים חוסמים.

**ניגודיות וצבע**
- ניגודיות מינימלית לגוף טקסט **7:1**, לכותרות 4.5:1. אין אפור על אפור.
- כל מידע שמקודד בצבע מקודד גם בצורה או בטקסט.

**עמידות**
- מחיקה = אישור אחד + ביטול זמין 10 שניות.
- אין פעולות שמסתמכות על זיכרון — כל מצב מוצג על המסך.
- תמונות מוצרים 72×72 לפחות. זיהוי חזותי מהיר מקריאה.
- שדות טקסט רגילים, כך שהכתבה קולית של iOS עובדת מהקופסה.

## 9. איך זה מגיע לאייפון

| גישה | עלות | תוקף | עדכונים | |
|---|---|---|---|---|
| **PWA למסך הבית** | — | **קבוע** | מיידיים, בלי מגע בטלפון שלהם | ✅ **הבחירה** |
| TestFlight | $99/שנה | פג כל 90 יום | דורש מהם לעדכן ידנית | ❌ |
| Xcode, חשבון חינמי | — | פג כל 7 ימים | חיבור פיזי למחשב | ❌ |

TestFlight נופל על ה־90 יום: כל רבעון מישהו נכנס לטלפון של ההורים ומעדכן. חשבון חינמי — שבוע ונגמר.

PWA נותן אייקון על מסך הבית, מסך מלא, התראות פוש, עבודה לא־מקוונת, ועדכונים שאתה דוחף בלי לגעת במכשירים שלהם. שניהם על iOS עדכני, כך שפוש זמין.

## 10. תשתית — GitHub + Supabase

הכל על שני החשבונות הקיימים. אין שירות שלישי, אין חשבון חדש.

| שכבה | בחירה | הערה |
|---|---|---|
| קוד | **GitHub** | מקור אמת יחיד |
| אחסון האתר | **GitHub Pages** | פריסה אוטומטית ב־GitHub Actions בכל דחיפה ל־main |
| בסיס נתונים | **Supabase Postgres** | כולל RLS |
| סנכרון בזמן אמת | **Supabase Realtime** | `postgres_changes` על טבלת הפריטים |
| זיהוי | **Supabase Auth** — כניסה אנונימית + קוד משק בית | בלי סיסמאות |
| קבצים | **Supabase Storage** | תמונות מוצרים ומתכונים |
| התראות | **Supabase Edge Function** + Web Push (VAPID) | מופעל מ־Database Webhook |
| ממשק | React + Vite + `vite-plugin-pwa` | סטטי לחלוטין |

**שתי הערות תפעוליות:**

1. **GitHub Pages בריפו פרטי דורש מנוי בתשלום.** בחשבון חינמי, Pages עובד רק בריפו ציבורי. זה בסדר כאן — המפתח היחיד בקוד הוא ה־`anon key` של Supabase, שנועד להיות פומבי ומוגן ב־RLS. אם מעדיפים ריפו פרטי, זה דורש GitHub Pro.
2. **פרויקט Supabase חינמי נכנס להשהיה אחרי שבוע ללא פעילות.** האפליקציה בשימוש שבועי — בדיוק על הגבול. חופשה או חג ידחפו אותה מעבר לו, והאפליקציה תישבר בדיוק כשההורים ינסו להשתמש בה. **המענה: GitHub Actions עם `schedule` יומי ששולח בקשה קלה ל־Supabase.** עולה כלום, משתמש רק בחשבונות הקיימים, ומסיר את הסיכון לגמרי. זה חלק משלב 0, לא שיפור עתידי.

### 10.1 מה משתנה לעומת התכנון הקודם

הפער האמיתי היחיד בין Supabase ל־Firestore לצורך הזה: **Firestore נותן עבודה לא־מקוונת בחינם. Supabase לא.** ל־`supabase-js` אין שכבת מטמון מקומית ואין תור כתיבות.

זה נוגע בדיוק בדרישה הקשה ביותר — אבא מסמן מוצרים בסופר, איפה שאין קליטה. לכן שכבת האופליין עוברת מ"תכונה של הספרייה" ל"רכיב שאנחנו בונים". ראה סעיף 12.

היקף העבודה הזו מוגבל ומוגדר היטב, כי מה שצריך לעבוד אופליין הוא צר: אבא משנה סטטוס של שורות ברשימה אחת. אמא בבית על רשת יציבה. זה לא סנכרון דו־כיווני כללי — זה תור של עדכוני סטטוס אידמפוטנטיים.

**בתמורה** Postgres נותן משהו ש־Firestore לא: מיזוג המרכיבים הופך משאילתה בצד הלקוח לשאילתת SQL אחת, במקום אחד, שאפשר לבדוק.

## 11. בסיס הנתונים

### 11.1 סכימה

```sql
create type item_status as enum ('pending','bought','missing','substituted');
create type member_role as enum ('planner','shopper','admin');

create table households (
  id uuid primary key default gen_random_uuid(),
  name text not null,
  join_code text unique not null,               -- 6 ספרות
  department_order text[] not null,             -- סדר ההליכה של אבא
  created_at timestamptz default now()
);

create table members (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households on delete cascade,
  user_id uuid not null references auth.users on delete cascade,
  name text not null,
  role member_role not null,
  text_scale text default 'large',
  unique (household_id, user_id)
);

create table products (                          -- הקטלוג, מצטבר לאורך זמן
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households on delete cascade,
  name text not null,
  brand text default '',
  image_url text,
  department_key text not null,
  default_unit text not null,
  aliases text[] default '{}',                  -- לחיפוש: "עגבניה", "שרי"
  usage_count int default 0,                    -- ממיין תוצאות חיפוש
  is_seed boolean default false
);
create index on products using gin (
  to_tsvector('simple', name || ' ' || array_to_string(aliases,' '))
);

create table recipes (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households on delete cascade,
  name text not null,
  image_url text,
  note text default '',
  times_used int default 0,
  last_used_at timestamptz
);

create table recipe_ingredients (
  id uuid primary key default gen_random_uuid(),
  recipe_id uuid not null references recipes on delete cascade,
  product_id uuid not null references products on delete restrict,
  quantity numeric not null,
  unit text not null,
  note text default ''
);

create table lists (
  id uuid primary key default gen_random_uuid(),
  household_id uuid not null references households on delete cascade,
  title text not null,
  status text not null default 'draft',         -- draft|sent|shopping|done
  created_by uuid references auth.users,
  created_at timestamptz default now(),
  sent_at timestamptz,
  done_at timestamptz
);

create table list_recipes (
  list_id uuid references lists on delete cascade,
  recipe_id uuid references recipes on delete cascade,
  primary key (list_id, recipe_id)
);

create table list_items (
  id uuid primary key default gen_random_uuid(),
  list_id uuid not null references lists on delete cascade,
  household_id uuid not null references households on delete cascade,
  product_id uuid references products on delete set null,

  -- צילום רגעי: רשימות עבר לא משתנות אם המוצר ישונה בקטלוג
  name_snapshot text not null,
  brand_snapshot text default '',
  image_snapshot text,
  department_key text not null,

  quantities jsonb not null default '[]',       -- [{"value":1.5,"unit":"kg"}]
  source_recipes jsonb not null default '[]',   -- [{"id":"…","name":"שניצל"}]
  is_extra boolean default false,
  sort_index int not null,

  status item_status not null default 'pending',
  substitute_note text default '',
  substitute_image_url text,
  planner_reply text default '',

  updated_by uuid references auth.users,
  updated_at timestamptz default now(),
  client_updated_at timestamptz default now()   -- הכרעת התנגשויות, ראה 12.2
);
create index on list_items (list_id, sort_index);

create table push_subscriptions (
  id uuid primary key default gen_random_uuid(),
  user_id uuid not null references auth.users on delete cascade,
  household_id uuid not null references households on delete cascade,
  endpoint text unique not null,
  p256dh text not null,
  auth text not null
);

create table units (                             -- טבלת ייחוס למיזוג
  unit text primary key,                         -- kg, g, l, ml, unit, pack…
  family text not null,                          -- mass, volume, count_unit…
  factor numeric not null                        -- ל־kg: 1000, ל־g: 1
);
```

### 11.2 הרשאות (RLS)

כל הטבלאות עם `enable row level security`. כדי להימנע מתת־שאילתה בכל בדיקה, `household_id` נדחף לתוך ה־JWT דרך Custom Access Token Hook, והמדיניות הופכת להשוואה אחת:

```sql
create policy household_isolation on list_items
  for all
  using      (household_id = (auth.jwt() ->> 'household_id')::uuid)
  with check (household_id = (auth.jwt() ->> 'household_id')::uuid);
```

אותה מדיניות על כל טבלה שיש בה `household_id`. משתמש לא יכול לראות או לגעת בשום דבר מחוץ למשק הבית שלו, ואין דרך לעקוף את זה מהלקוח — גם עם ה־`anon key` הפומבי.

### 11.3 מיזוג המרכיבים

פונקציית Postgres אחת בונה את הרשימה מהמתכונים הנבחרים:

```
build_list(household_id, recipe_ids[]) →
  1. אוסף את כל recipe_ingredients של המתכונים
  2. מצרף ל־units כדי לקבל family + factor
  3. group by (product_id, family) → sum(quantity * factor)
  4. ממיר חזרה ליחידה הנוחה ובונה את מערך quantities
  5. אוסף את שמות המתכונים אל source_recipes
  6. מחשב sort_index לפי מיקום department_key ב־household.department_order
  7. מוסיף את השורות ל־list_items
```

לוגיקת המיזוג יושבת במקום אחד, ליד הנתונים, וניתנת לבדיקה ב־SQL בלי להריץ את האפליקציה.

### 11.4 בזמן אמת

```js
supabase.channel(`list:${listId}`)
  .on('postgres_changes',
      { event: '*', schema: 'public', table: 'list_items',
        filter: `list_id=eq.${listId}` },
      applyRemoteChange)
  .subscribe()
```

**חשוב:** Realtime לא מבטיח מסירה של אירועים שקרו בזמן ניתוק. בכל התחברות מחדש מתבצעת שליפה מלאה של הרשימה והתאמה מול המצב המקומי. בלי זה, אבא חוזר מהמרתף ומפספס את התשובה של אמא.

## 12. שכבת האופליין

הרכיב שאנחנו בונים בעצמנו, ובלעדיו המוצר מת בשבוע הראשון.

### 12.1 קריאה

- ברשת: שליפת הרשימה והפריטים, וכתיבה ל־IndexedDB.
- בלי רשת: קריאה מ־IndexedDB. אבא פותח את האפליקציה במרתף ורואה רשימה מלאה.
- **תמונות המוצרים חייבות להישמר במטמון של ה־Service Worker** (Workbox, `CacheFirst`). בלי זה הרשימה האופליינית היא טקסט בלבד, ומאבדת בדיוק את היתרון של זיהוי חזותי מהיר.

### 12.2 כתיבה — תור יוצא

כל שינוי סטטוס עושה שלושה דברים:

1. מעדכן את המצב המקומי מיד. הממשק לא ממתין לרשת אף פעם.
2. כותב שורה ל־`outbox` ב־IndexedDB: `{ itemId, patch, clientUpdatedAt, attempts }`.
3. מנסה לשלוח.

המשגר רץ בעליית האפליקציה, באירוע `online`, וכל 15 שניות כל עוד יש ממתינים. הצלחה מוחקת מהתור, כישלון משאיר עם השהיה גדלה.

הכתיבה עוברת דרך פונקציה שמכריעה התנגשויות לפי חותמת הזמן של הלקוח:

```sql
create function apply_item_update(p_item uuid, p_patch jsonb, p_ts timestamptz)
-- מתעלמת מהעדכון אם p_ts ישן מ־client_updated_at הקיים
```

התוצאה: העדכונים אידמפוטנטיים ולא תלויי סדר. אפשר לשגר את אותו עדכון פעמיים, או להגיע באיחור אחרי עדכון חדש יותר, בלי לדרוס כלום. שני אנשים שעורכים את אותו פריט הוא לא תרחיש אמיתי כאן, אבל הכלל מוגדר: הכתיבה האחרונה לפי שעון הלקוח מנצחת.

### 12.3 מה המשתמש רואה

- שורת מצב: **"הכל מסונכרן ✓"** / **"לא מחובר — נשמר במכשיר (3)"**.
- אף פעם לא ספינר חוסם, אף פעם לא הודעת שגיאה על חוסר רשת. חוסר קליטה בסופר הוא מצב תקין, לא תקלה.
- `Screen Wake Lock` — המסך של אבא לא נכבה כל 30 שניות באמצע הקנייה.

### 12.4 מימוש

TanStack Query עם מתמיד ל־IndexedDB לצד הקריאה, ותור קטן בכתיבה. בלי שירות סנכרון חיצוני ובלי חשבון נוסף. ההיקף המשוער: כמה מאות שורות, מרוכזות במודול אחד.

## 13. התראות

- **Database Webhook** על `list_items` (שינוי סטטוס) ועל `lists` (מעבר ל־`sent`) קורא ל־**Edge Function**.
- הפונקציה שולפת את המנוי של הנמען מ־`push_subscriptions` ושולחת Web Push עם מפתחות VAPID. המפתח הפרטי הוא secret של הפונקציה.
- אין צורך ב־Firebase. Web Push הוא תקן, ו־iOS תומך בו למי שהתקין את האפליקציה למסך הבית.
- **תנאי:** פוש ב־iOS עובד רק אחרי הוספה למסך הבית. זה חלק מטקס ההתקנה הפיזי מולם, פעם אחת.
- **גיבוי בוואטסאפ:** כפתור שמייצר טקסט נקי של הרשימה לפי מחלקות. גם ההתראה הכי אמינה נכשלת לפעמים, וזה ערוץ שהם כבר בתוכו יום־יום.

## 14. תמונות מוצרים

שני מקורות, כפי שביקשת:

1. **קטלוג זרוע** — ~250 מוצרים ישראליים נפוצים עם תמונה ומחלקה, מוכנים מראש. מכסה את הרוב המוחלט.
2. **צילום** — כפתור מצלמה בכל מוצר. `<input type="file" accept="image/*" capture="environment">` פותח את המצלמה ישירות.
   - התמונה **מוקטנת ל־800px ונדחסת בדפדפן לפני ההעלאה**. תמונת 12 מגה־פיקסל דרך רשת סלולרית בסופר לא תעלה, ואבא יחשוב שהאפליקציה תקועה.
   - צילום זמין גם לאבא: "החלפתי" מאפשר לצלם את מה שהוא כן לקח, ואמא רואה בדיוק מה נכנס לעגלה.

## 15. מה לא בגרסה הראשונה

- סריקת ברקוד
- השוואת מחירים או ריבוי רשתות
- ייבוא מתכונים מאתרי אינטרנט
- לוח מנות שבועי / תכנון תפריט
- ניהול מלאי במזווה
- יותר משלושה משתמשים או ממשק בית אחד
- העלאה לחנות האפליקציות

כל אחד מאלה מוסיף מסך או החלטה, ומסך נוסף הוא בדיוק מה שיחזיר אותם לדף ולעט.

## 16. תוכנית עבודה

| שלב | תוכן | תוצר |
|---|---|---|
| **0 — תשתית** | פרויקט Supabase, סכימה, RLS, כניסה בקוד. GitHub Actions לפריסה ל־Pages **ולשמירת הפרויקט ער**. קטלוג זרוע ~250 מוצרים. **הזנת 20 המתכונים של אמא מראש.** | האפליקציה נפתחת בטלפון, מחוברת, ועם כל התוכן של אמא כבר בפנים |
| **1 — הליבה** | בחירת מנות, `build_list`, מסך אבא, סימון "נקנה", Realtime | המסלול המלא עובד מקצה לקצה |
| **2 — אופליין** | מטמון IndexedDB, תור יוצא, `apply_item_update`, מטמון תמונות, שורת מצב | עובד במרתף של חצי חינם |
| **3 — ערוץ חזרה** | חסר / הוחלף + צילום, תשובות של אמא, מעקב חי, סיכום | אין יותר צורך בשיחת טלפון |
| **4 — התאמות** | גרירת סדר מחלקות, גודל טקסט, פוש, שיתוף בוואטסאפ | מוכן להתקנה אצלם |
| **5 — הרצה** | התקנה פיזית על שני המכשירים, הדרכה, ליווי שבועיים | הם משתמשים בזה לבד |

**מה שהתשובה שלך על 20 המתכונים משנה:** במקום שאמא תעמוד מול מסך ריק ותקליד את המתכון הראשון שלה, אני מזין את כל ה־20 בשלב 0. בשימוש הראשון היא פותחת את האפליקציה ורואה את כל הרפרטואר שלה עם תמונות, ורק מקישה על ארבעה. הסיכון הגדול ביותר במוצר — שהיא לא תעבור את ההזנה הראשונה — נמחק לגמרי.

בשביל זה אני צריך את הרשימה: 20 שמות המנות, והמרכיבים והכמויות של כל אחת. שיחה אחת איתה, או דף שהיא תמלא.

**האופליין עלה לשלב נפרד ומוקדם.** ב־Firestore הוא היה נלווה; כאן הוא רכיב, והוא קודם לערוץ החזרה כי בלעדיו כל השאר לא שווה כלום בתוך הסופר.

## 17. סיכונים

| סיכון | חומרה | מענה |
|---|---|---|
| **פרויקט Supabase נכנס להשהיה אחרי שבוע** | **גבוה** | Cron יומי ב־GitHub Actions ששומר אותו ער. חלק משלב 0 |
| הרגל — דף ועט עובדים מצוין | **גבוה** | 20 המתכונים מוזנים מראש; מהשבוע הראשון זה כבר מהיר מהדף |
| אין קליטה בסופר, ואין אופליין מובנה | **גבוה** | שלב 2 ייעודי. תור יוצא + מטמון תמונות |
| סדר המחלקות בחצי חינם לא מדויק | בינוני | שאלה אחת לאבא, או גרירה אחת |
| Pages דורש ריפו ציבורי בחשבון חינמי | בינוני | הריפו ציבורי; ה־`anon key` נועד להיות פומבי ו־RLS מגן |
| פוש דורש התקנה למסך הבית | בינוני | חלק מטקס ההתקנה הפיזי. וואטסאפ כגיבוי |
| המסך של אבא נכבה באמצע | בינוני | `Screen Wake Lock` |
| הם "יאבדו" את האפליקציה | בינוני | אייקון גדול ומובחן, שם בעברית, במסך הבית הראשון ליד וואטסאפ |

## 18. שאלות פתוחות

1. **20 המתכונים של אמא** — שמות, מרכיבים וכמויות. זה החסם היחיד לשלב 0.
2. **סדר המחלקות בחצי חינם** — מה אבא עובר קודם, דלפק הגבינות או הקצבייה?
3. **ריפו ציבורי או GitHub Pro?** משפיע רק על נראות הקוד, לא על אבטחת הנתונים.
4. **יש עוד מישהו במשפחה שצריך גישה?** אח או אחות שקונים לפעמים משנים את מודל ההרשאות.

---

## נספח א׳ — מסכים

**אמא** — בית ("מה מבשלים לשבת?") · עורך מתכון · אישור רשימה · מעקב חי
**אבא** — הרשימה · פרטי פריט · סיכום
**משותף** — הגדרות (גודל טקסט, סדר מחלקות, קוד משק בית)
