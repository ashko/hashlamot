# השלמות — אפיון מוצר

> אפליקציית רשימת קניות משותפת להורים. אמא מתכננת את הבישולים, אבא קונה, שניהם רואים את אותו הדבר בזמן אמת.

**גרסה:** 0.1 — טיוטה לדיון
**סטטוס:** ממתין לאישור לפני תחילת פיתוח

---

## 1. הבעיה

מדי יום רביעי אמא מתחילה בהכנות לבישולי סוף השבוע. היא יודעת אילו מנות היא רוצה להכין, ומהמנות האלה היא גוזרת בראש רשימת מרכיבים, כותבת אותה ביד על דף, ומעבירה לאבא. אבא לוקח את הדף לסופר ועובר בין המדפים.

מה שבור בתהליך הזה:

| הכאב | מה קורה היום |
|---|---|
| עבודה כפולה | אותם מרכיבים נכתבים מחדש כל שבוע, בכתב יד |
| סדר לא נכון | הרשימה מסודרת לפי מתכון, אבל הסופר מסודר לפי מחלקות — אבא מסתובב הלוך ושוב |
| אין ערוץ חזרה | אם מוצר חסר במדף, אבא צריך להתקשר, או לנחש, או לוותר |
| שכחות | פריט שנשמט מהדף מתגלה רק כשמתחילים לבשל |
| אין זיכרון | "כמה עגבניות קונים לקוגל?" — התשובה נמצאת רק בראש של אמא |

## 2. המטרה

לצמצם את התכנון של אמא מ־20 דקות של כתיבה ידנית לפחות מ־3 דקות של הקשות, ולתת לאבא רשימה שמסודרת לפי הדרך שהוא הולך בסופר — עם ערוץ חזרה חי כשמשהו חסר.

**מדדי הצלחה:**

1. אמא בונה רשימה שלמה לשבת בפחות מ־3 דקות, לבד, בלי עזרה.
2. אבא משלים קנייה בלי אף שיחת טלפון של הבהרה.
3. אפס פריטים נשכחים בשבועיים רצופים.
4. שניהם משתמשים באפליקציה שבוע רביעי ברציפות — בלי שנזכיר להם.

המדד הרביעי הוא היחיד שבאמת חשוב. דף ועט זה ממשק מצוין; אפליקציה מנצחת אותו רק אם היא מהירה יותר ממנו כבר בשבוע השני.

## 3. המשתמשים

| מי | תפקיד במערכת | מה הוא עושה |
|---|---|---|
| **אמא** | `planner` | בוחרת מנות, מאשרת רשימה, עונה כשחסר משהו |
| **אבא** | `shopper` | מקבל רשימה, מסמן מה נקנה, מדווח על חוסרים |
| **אתה** | `admin` | הקמה ראשונית, קטלוג מוצרים, תמיכה מרחוק |

הפרדת התפקיד השלישי היא לא פרט טכני — היא הנחת יסוד עיצובית. אף אחת מפעולות התחזוקה (ניהול קטלוג, סדר מחלקות, גיבוי) לא נופלת עליהם. הם רואים שני מסכים בסך הכל.

## 4. המסע — יום רביעי

זה לב האפיון. כל שאר המסמך משרת את הרצף הזה.

### 4.1 אמא, בוקר יום רביעי

1. פותחת את האפליקציה. שאלה אחת גדולה על המסך: **"מה מבשלים לשבת?"**
2. מתחתיה רשת של המתכונים שלה, כל אחד עם תמונה ושם. היא מקישה על שניצל, מרק עוף, קוגל, סלט חצילים. כל אחד שנבחר מקבל מסגרת ירוקה ו־✓.
3. מתכון חדש? כפתור **"+ מנה חדשה"** → שם, תמונה (אופציונלי), ואז הוספת מרכיבים בחיפוש־תוך־כדי־הקלדה מתוך קטלוג מוצרים מוכן. **את זה היא עושה פעם אחת בלבד** — בשבוע הבא המתכון כבר שם עם כל המרכיבים.
4. מקישה **"הכנת רשימה"**. המערכת ממזגת את כל המרכיבים מכל המנות, מאחדת כפילויות, מחברת כמויות, ומסדרת לפי מחלקות בסופר.
5. מסך אישור: *"23 מוצרים"*. היא יכולה לשנות כמות, למחוק, ולהוסיף דברים שלא קשורים למתכון (חלב, נייר טואלט) — יש שדה **"+ להוסיף עוד משהו"** קבוע למעלה.
6. מקישה **"לשלוח לאבא"**.

### 4.2 אבא, בסופר

1. מקבל התראה: *"אמא שלחה רשימה — 23 מוצרים"*. מקיש עליה, האפליקציה נפתחת ישר על הרשימה.
2. הרשימה מסודרת לפי המחלקות **בסדר שהוא הולך בסופר** — קודם ירקות, אחר כך מאפייה, אחר כך קצבייה, וכן הלאה. כל מחלקה היא כותרת גדולה וצבעונית.
3. כל שורה: תמונת המוצר, שם גדול, מותג, וכמות. הקשה על השורה כולה = **נקנה**. השורה הופכת לירוקה עם ✓ ודוהה.
4. אם משהו לא בנמצא — שני כפתורים בשורה: **"חסר"** ו־**"החלפתי"**. "החלפתי" פותח שדה קצר ואפשרות לצלם את מה שהוא כן לקח.
5. למעלה, קבוע: פס התקדמות — *"12 מתוך 23"*.
6. בסוף — כפתור אחד גדול: **"סיימתי"**.

### 4.3 אמא, במקביל

- רואה בזמן אמת מה כבר נקנה. לא צריכה לשאול.
- כשאבא מסמן "חסר" היא מקבלת התראה מיידית: *"חסר: פטרוזיליה"* — ושלושה כפתורי תשובה: **"קח כוסברה במקום"** / **"לא צריך"** / **"תכתוב לי"**.
- התשובה מופיעה אצל אבא על השורה עצמה, בתוך שניות.
- כשהוא מסיים היא מקבלת סיכום: *"אבא סיים. נקנו 21, חסרים 2."*

## 5. הסידור לפי מחלקות בסופר

הדרישה המרכזית: **הרשימה מסודרת לפי הסופר, לא לפי המתכון.**

### 5.1 סדר ברירת מחדל

הסדר הבא הוא המסלול הטיפוסי בסופר ישראלי גדול. הוא ניתן לשינוי — ראה 5.2.

| # | מחלקה | # | מחלקה |
|---|---|---|---|
| 1 | פירות וירקות | 9 | שמן, רטבים וממרחים |
| 2 | מאפייה ולחמים | 10 | חטיפים, מתוקים ודגני בוקר |
| 3 | בשר, עוף ודגים | 11 | משקאות, קפה ותה |
| 4 | חלב וביצים | 12 | קפואים |
| 5 | גבינות ומעדנייה | 13 | ניקיון, כביסה וחד״פ |
| 6 | שימורים ומזון יבש | 14 | טואלטיקה ופארם |
| 7 | אורז, פסטה וקטניות | 15 | שונות |
| 8 | תבלינים, אפייה וסוכר | | |

### 5.2 התאמה לסופר של אבא

בהגדרות יש מסך אחד: רשימת המחלקות עם ידית גרירה. אבא (או אתה, איתו) גורר פעם אחת כדי להתאים לסופר שלו. הסדר נשמר ברמת משק הבית ותקף לכל הרשימות מכאן והלאה.

מחלקה ללא פריטים ברשימה הנוכחית פשוט לא מוצגת.

### 5.3 איך מוצר מקבל מחלקה

- **מוצרי הקטלוג הזרוע** (~250 מוצרים נפוצים) מגיעים עם מחלקה משויכת מראש. זה מכסה את הרוב המוחלט.
- **מוצר חדש שאמא יוצרת** — אחרי הקלדת השם מופיעה רשת של 15 אריחים גדולים עם אייקון וצבע. הקשה אחת. לא רשימה נפתחת.
- אם היא דילגה, המוצר נוחת ב"שונות" בסוף הרשימה. הוא לא הולך לאיבוד.

## 6. מיזוג כמויות

כששני מתכונים דורשים את אותו מוצר, הוא מופיע פעם אחת עם הכמות המחוברת.

**כללי החיבור:**

- כמויות מתחברות רק בתוך אותה משפחת יחידות: **משקל** (ק״ג/גרם), **נפח** (ליטר/מ״ל), **ספירה** (יחידה, חבילה, קופסה, צרור, שקית — כל אחת נפרד).
- ק״ג של מתכון אחד + גרם של אחר → מומרים לגרם, מחוברים, ומוצגים ביחידה הנוחה: `1000 + 500 = 1.5 ק״ג`.
- יחידות שלא ניתנות לחיבור מוצגות זו לצד זו על אותה שורה: `עגבניות — 2 ק״ג + 3 יחידות`.

**ניסוח אנושי של כמויות.** לא `0.5 ק"ג` אלא **"חצי קילו"**. לא `1.5` אלא **"קילו וחצי"**. שברים נפוצים (רבע, שליש, חצי) נכתבים במילים.

**מקור המרכיב.** מתחת לכל שורה, בקטן: *"לשניצל, למרק"*. זה עוזר לאבא כשהוא מתלבט, ולאמא כשהיא בודקת את הרשימה.

## 7. מצבי מוצר

לכל פריט ברשימה יש מצב אחד מארבעה:

| מצב | מי מסמן | מראה אצל אבא | מה קורה אצל אמא |
|---|---|---|---|
| `pending` — לקנות | ברירת מחדל | לבן, ריבוע ריק | נספר ב"נשאר" |
| `bought` — נקנה | אבא | ירוק, ✓, טקסט דהוי | מתקדם בפס ההתקדמות |
| `missing` — חסר | אבא | אדום, ✗ | **התראה מיידית** + כפתורי תשובה |
| `substituted` — הוחלף | אבא | כתום, טקסט התחליף ותמונה | התראה + פירוט מה נלקח |

המצב אף פעם לא מקודד בצבע בלבד — תמיד צבע **וגם** אייקון **וגם** שינוי טקסט. חלק מהמשתמשים לא מבחינים היטב בגוונים.

## 8. עקרונות ממשק לגיל השלישי

מספרים, לא תארים. אלה מחייבים.

**טיפוגרפיה**
- גוף טקסט: **20px** בסיס. הגדרת גודל גלובלית: רגיל 20 / גדול 23 / **ענק 27**. ברירת המחדל היא *גדול*.
- שם מוצר ברשימה: **26px**, משקל 600.
- כותרת מחלקה: **30px**, משקל 800, דביקה בראש הגלילה.
- כמות: **24px**, משקל 700, ספרות בעלות רוחב אחיד.
- ללא טקסט מתחת ל־17px בשום מקום, כולל הערות שוליים.

**מגע**
- אזור הקשה מינימלי: **68×68px**. שורת מוצר: **96px** גובה, כל הרוחב לחיץ.
- מרווח מינימלי בין שני יעדי הקשה: 12px. אין כפתורים צמודים.
- אין החלקה (swipe) כפעולה יחידה — כל פעולה זמינה גם בהקשה.
- אין הקשה כפולה, אין לחיצה ארוכה כפעולה יחידה.

**ניווט**
- שני מסכים ראשיים לכל היותר לכל משתמש. אין תפריט המבורגר, אין תפריטים מקוננים.
- כל מסך: פעולה ראשית **אחת**, כפתור ברוחב מלא בתחתית.
- כפתור חזרה תמיד גלוי, תמיד באותו מקום.
- אין חלונות קופצים חוסמים. שינוי שנשמר מציג הודעה קצרה ונעלמת — לא דורש אישור.

**ניגודיות וצבע**
- יחס ניגודיות מינימלי לגוף טקסט: **7:1**. לכותרות: 4.5:1.
- אין אפור על אפור. אין טקסט על תמונה בלי שכבת רקע.
- כל מידע שמקודד בצבע מקודד גם בצורה או בטקסט.

**עמידות**
- אי אפשר למחוק רשימה או מתכון בהקשה אחת. מחיקה = אישור אחד, ואפשרות ביטול למשך 10 שניות.
- אין פעולות שמסתמכות על זיכרון — כל מצב מוצג על המסך.
- תמונות מוצרים גדולות (72×72 לפחות). זיהוי חזותי מהיר מקריאה.
- הקלדה קולית: כל שדה טקסט הוא שדה רגיל, כך שהכתבה של iOS עובדת מהקופסה.

## 9. ארכיטקטורה

### 9.1 איך זה מגיע לאייפון — המלצה: PWA

בקשת המוצא היא אפליקציה שלא עולה לחנות. שלוש הדרכים:

| גישה | חשבון מפתח | תוקף | עדכונים | המלצה |
|---|---|---|---|---|
| **PWA למסך הבית** | לא נדרש | **קבוע** | מיידיים, בלי מגע בטלפון שלהם | ✅ **זו הבחירה** |
| TestFlight | $99 לשנה | פג כל 90 יום | דורש מהם לעדכן ידנית | ❌ |
| Xcode עם חשבון חינמי | לא נדרש | **פג כל 7 ימים** | חיבור פיזי למחשב | ❌ |

TestFlight נופל על ה־90 יום: כל רבעון מישהו צריך להיכנס לטלפון של ההורים ולעדכן. חשבון חינמי בלתי אפשרי — שבוע ונגמר.

PWA נותן: אייקון על מסך הבית, מסך מלא בלי סרגל דפדפן, התראות פוש (iOS 16.4 ומעלה, בתנאי שהותקן למסך הבית), עבודה במצב לא־מקוון, ועדכונים שאתה דוחף מהמחשב שלך בלי לגעת במכשירים שלהם. המחיר: אין גישה למצלמה ברמת אפליקציה מקומית — אבל `<input type="file" capture>` פותח את המצלמה, וזה כל מה שצריך לצילום מוצר.

### 9.2 מחסנית

| שכבה | בחירה | למה |
|---|---|---|
| ממשק | React + Vite, PWA | מהיר, סטנדרטי, קל להחזיק לאורך זמן |
| נתונים | **Cloud Firestore** | סנכרון בזמן אמת + מצב לא־מקוון בקופסה אחת |
| זיהוי | Firebase Anonymous Auth + קוד משק בית | בלי סיסמאות, בלי אימיילים |
| קבצים | Firebase Storage | תמונות מוצרים ומתכונים |
| התראות | Web Push (FCM) + Cloud Function | טריגר על שינוי מסמך |
| אחסון | **Netlify** | פריסה מגיט, HTTPS, בחינם |

Firestore הוא הבחירה המרכזית והוא פותר את שתי הדרישות הקשות בבת אחת: סעיף 7 (סנכרון בזמן אמת) וסעיף הקליטה בסופר. אין צורך בשרת.

### 9.3 מצב לא־מקוון — דרישה קשיחה

בסופר, בעיקר במרתפים ובקומות תחתונות, אין קליטה. זה לא מקרה קצה — זה תנאי הפעלה.

- Firestore עם `persistentLocalCache`. כל סימון של אבא נכתב מקומית ומסתנכרן כשחוזרת קליטה.
- שורת מצב גלויה: **"לא מחובר — נשמר במכשיר"** / **"הכל מסונכרן ✓"**.
- Screen Wake Lock API — המסך של אבא לא נכבה כל 30 שניות באמצע הקנייה.

### 9.4 התראות ומסלול גיבוי

- **פוש**: כשנשלחת רשימה חדשה, כשמסומן חוסר או תחליף, וכשאבא מסיים.
- **דרישה**: פוש ב־iOS עובד רק אם ה־PWA הותקנה למסך הבית. ההתקנה חייבת להיעשות פיזית מולם, פעם אחת.
- **מסלול גיבוי — וואטסאפ**: כפתור "שליחה בוואטסאפ" מייצר טקסט נקי של הרשימה, מסודר לפי מחלקות. גם ההתראה הכי אמינה נכשלת לפעמים; זו רשת הביטחון, והיא בערוץ שהם כבר משתמשים בו יום־יום.

### 9.5 זיהוי ופרטיות

- כניסה אנונימית + **קוד משק בית בן 6 ספרות**, מוקלד פעם אחת בהתקנה. אין סיסמאות, אין מסך התחברות, אין "שכחתי סיסמה".
- המכשיר נשאר מחובר לצמיתות.
- כללי Firestore: משתמש קורא וכותב רק תחת משק הבית שבו יש לו מסמך חברות.
- הנתונים היחידים שנשמרים: שמות מוצרים, מתכונים, ותמונות שהם צילמו. אין מידע אישי מזהה מעבר לשמות "אמא"/"אבא".

## 10. מודל נתונים

```
households/{hid}
  name, joinCode, departmentOrder[], createdAt

households/{hid}/members/{uid}
  name, role: planner|shopper|admin, textScale, pushSubscription

households/{hid}/products/{pid}          ← הקטלוג, מצטבר לאורך זמן
  name, brand, imageUrl, department,
  defaultUnit, aliases[], usageCount, isSeed

households/{hid}/recipes/{rid}
  name, imageUrl, note, lastUsedAt, timesUsed
  ingredients[]: { productId, quantity, unit, note }

households/{hid}/lists/{lid}
  title, status: draft|sent|shopping|done,
  recipeIds[], createdAt, sentAt, doneAt,
  stats: { total, bought, missing, substituted }

households/{hid}/lists/{lid}/items/{iid}
  productId, nameSnapshot, brandSnapshot, imageSnapshot, department
  quantities[]: { value, unit }        ← מערך, לטיפול ביחידות שלא מתחברות
  sourceRecipes[]: { recipeId, name }
  isExtra, sortIndex
  status, substituteNote, substituteImageUrl, plannerReply
  updatedAt, updatedBy
```

**שתי החלטות שכדאי לשים לב אליהן:**

1. **שדות ה־snapshot בפריט.** שם המוצר, המותג והתמונה מועתקים לתוך פריט הרשימה ולא נקראים מהקטלוג. אם אמא תשנה שם מוצר בעוד חודשיים, רשימות עבר לא ישתנו רטרואקטיבית.
2. **`usageCount` על המוצר.** תוצאות החיפוש מסודרות לפי מה שהיא באמת קונה, לא לפי אלפבית. אחרי חודש, שלוש התוצאות הראשונות הן כמעט תמיד הנכונות.

## 11. מה לא בגרסה הראשונה

הגבול הזה הוא חלק מהאפיון, לא השמטה:

- סריקת ברקוד
- השוואת מחירים או ריבוי רשתות
- ייבוא מתכונים מאתרי אינטרנט
- לוח מנות שבועי / תכנון תפריט
- ניהול מלאי במזווה
- יותר משלושה משתמשים או יותר ממשק בית אחד
- העלאה לחנות האפליקציות

כל אחד מאלה מוסיף מסך או החלטה, ומסך נוסף הוא בדיוק מה שיגרום להם לחזור לדף ולעט.

## 12. תוכנית עבודה

| שלב | תוכן | תוצר |
|---|---|---|
| **0 — תשתית** | שלד PWA, Firebase, כניסה בקוד, **קטלוג זרוע של ~250 מוצרים ישראליים** עם תמונות ומחלקות | האפליקציה נפתחת בטלפון, מחוברת למשק בית |
| **1 — הליבה** | מתכונים, בחירת מנות, מיזוג לרשימה, מסך אבא, סימון "נקנה", סנכרון בזמן אמת | המסלול המלא עובד מקצה לקצה |
| **2 — ערוץ חזרה** | חסר / הוחלף, תשובות של אמא, מסך מעקב חי, מסך סיכום | אין יותר צורך בשיחת טלפון |
| **3 — התאמות** | גרירת סדר מחלקות, צילום מוצר, גודל טקסט, שיתוף בוואטסאפ, התראות פוש | מוכן להתקנה אצלם |
| **4 — הרצה** | התקנה פיזית על שני המכשירים, הדרכה, ליווי שבועיים, תיקונים | הם משתמשים בזה לבד |

שלב 0 הוא הקריטי ביותר ולא נראה ככה. הקטלוג הזרוע הוא ההבדל בין "אמא מקלידה 40 מוצרים מאפס בפעם הראשונה ונוטשת" לבין "היא מקלידה שתי אותיות ובוחרת מרשימה". זו העבודה שקובעת אם המוצר ישרוד את השבוע הראשון.

## 13. סיכונים

| סיכון | חומרה | מענה |
|---|---|---|
| הרגל — דף ועט עובדים מצוין | **גבוה** | קטלוג זרוע + שמירת מתכונים, כדי שמהשבוע השני זה יהיה מהיר מהדף |
| אמא לא תזין את המתכונים הראשונים | **גבוה** | להזין איתה 5–6 מתכונים יחד בהתקנה. לא להשאיר אותה מול מסך ריק |
| אין קליטה בסופר | גבוה | מצב לא־מקוון מהיום הראשון, לא כשיפור עתידי |
| פוש ב־iOS דורש התקנה למסך הבית | בינוני | חלק מטקס ההתקנה הפיזי. וואטסאפ כגיבוי |
| המסך של אבא נכבה באמצע | בינוני | Wake Lock |
| הם "יאבדו" את האפליקציה | בינוני | אייקון גדול ומובחן, שם בעברית, במסך הבית הראשון ליד וואטסאפ |
| אייפון ישן מ־iOS 16.4 | נמוך | האפליקציה עובדת, רק בלי פוש. וואטסאפ מכסה |

## 14. שאלות פתוחות

1. **באיזה סופר אבא קונה?** קובע את סדר המחלקות ההתחלתי.
2. **כמה מתכונים בערך ברפרטואר הקבוע?** אם זה 15–20, אפשר להזין את כולם מראש בשלב 0 ולחסוך לה את ההזנה הראשונה לגמרי.
3. **איזה דגם אייפון ואיזו גרסת iOS יש לכל אחד מהם?** קובע אם פוש זמין.
4. **תמונות מוצרים** — קטלוג זרוע בלבד, או שאמא מצלמת מוצרים מיוחדים בעצמה?
5. **האם יש עוד מישהו במשפחה שצריך גישה?** (אח/אחות שקונה לפעמים)

---

## נספח א׳ — הצעה למסכים

**אמא**
1. בית — "מה מבשלים לשבת?" + רשת מתכונים + כפתור רשימה
2. עורך מתכון — שם, תמונה, מרכיבים
3. אישור רשימה — הפריטים הממוזגים, עריכה, שליחה
4. מעקב חי — התקדמות אבא, חוסרים, תשובות

**אבא**
1. הרשימה — לפי מחלקות, סימון, פס התקדמות
2. פרטי פריט — תמונה גדולה, חסר / החלפתי
3. סיכום — "סיימתי"

**משותף**
4. הגדרות — גודל טקסט, סדר מחלקות, קוד משק בית
